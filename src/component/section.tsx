import React from 'react';

const Section = () => {
    return (
        <section className="container bg-orange-400 py-5 max-w-full flex justify-center">
            <h2 className="font-bold text-lg text-black">여기는 섹션입니다</h2>
        </section>
    );
};

export default Section;